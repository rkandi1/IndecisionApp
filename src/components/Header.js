import React from 'react';

// Need when we don't need to directly change any states.
const Header = (props) => (
    <div className="header">
      <div className="container">
        <h1 className="header__title"> Indecision </h1>
        <h2 className="header__subtitle"> If You're Lazy Enough to Automate Trivial Decision Making </h2>
      </div>
    </div>
  );

export default Header;
